Se ha creado una API para gestionar los eventos que tienen dentro de la ciudad de Zaragoza.

# Pasos lanzar la API 

1. dokcer compose -f docker-compose.dev.yaml up -d --> levantar el contenedor en el cual está la base de datos
2. mvn spring-boot:run --> lanza la API
